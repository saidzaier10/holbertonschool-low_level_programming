#include <stdlib.h>
#include <time.h>
#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	int (n, lastdigit);

	srand(time(0));
	n = rand() - RAND_MAX / 2;
	lastdigit = n % 10;

	if ()
	{	printf();
	}

	else if()
	{	printf();
	}

	else if()
	{	printf();
	}

	return (0);
}
