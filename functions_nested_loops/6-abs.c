#include "main.h"
/**
* _abs - computes the absolute value of an integer
*@value: number being absolute
*Return: 0
*/
int _abs(int value)
{
	if (value > 0)
	return (value);

	else if (value < 0)
	return (-value);

	else
	return (0);
}
