#include <stdio.h>
/**
 * La fonction principale du programme.
 *
 * @param argc Le nombre d'arguments de la ligne de commande.
 * @param argv Un tableau de pointeurs vers des chaînes de caractères contenant les arguments de la ligne de commande.
 *
 * @return 0 en cas de succès, une valeur non nulle en cas d'erreur.
 */
int main(int argc, char *argv[])
{
	printf("%s\n", argv[0]);
	return (0);
}
